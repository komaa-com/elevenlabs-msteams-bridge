{
  "name": "elevenlabs-msteams-bridge",
  "version": "0.1.0",
  "description": "Bridge provider: terminates the OpenClawBridge worker wire protocol on one side and the ElevenLabs Agent WebSocket on the other. Brings Microsoft Teams voice/video calls to an ElevenLabs agent.",
  "type": "module",
  "main": "dist/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
